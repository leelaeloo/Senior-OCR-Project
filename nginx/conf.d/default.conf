# --- HTTP to HTTPS Redirect ---
server {
    listen 80;
    server_name goodboy.kakaolab.cloud;
    return 301 https://$host$request_uri;
}

# --- HTTPS Server ---
server {
    listen 443 ssl;
    server_name goodboy.kakaolab.cloud;

    # --- SSL Certificates ---
    ssl_certificate /etc/letsencrypt/live/goodboy.kakaolab.cloud/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/goodboy.kakaolab.cloud/privkey.pem;

    # --- SSL Settings ---
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # --- Security Headers ---
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header Referrer-Policy "strict-origin-when-cross-origin";
    add_header X-XSS-Protection "1; mode=block";

    # --- Frontend (Next.js) ---
    location / {
        proxy_pass http://frontend:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_cache_bypass $http_upgrade;
    }

    # --- Backend (FastAPI) ---
    # /api 요청을 백엔드 root로 전달 (/api/ocr → /ocr)
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 파일 업로드 크기 제한
        client_max_body_size 10M;
    }

    # --- Error Pages ---
    error_page 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
